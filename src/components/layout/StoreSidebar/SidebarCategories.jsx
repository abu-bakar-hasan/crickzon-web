'use client';

/**
 * SidebarCategories
 * Scaffold for future quick-access category shortcuts in the store sidebar.
 * Currently a no-op; category navigation is handled via the StoreSidebar bottom section.
 */
export default function SidebarCategories() {
  return null;
}
